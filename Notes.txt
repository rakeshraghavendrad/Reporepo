import re

# Remove code block markers and optional 'json' prefix
cleaned_response = re.sub(r"^```(?:json)?\s*|\s*```$", "", response_text.strip())

# Parse cleaned JSON
json_result = json.loads(cleaned_response)
