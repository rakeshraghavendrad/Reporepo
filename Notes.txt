from deep_eval.metrics import ContextualPrecisionMetric

# Initialize Contextual Precision metric with custom instructions
ContextualPrecisionMetric_metric = ContextualPrecisionMetric(
    threshold=0.7,
    model=llm_no_ssl,
    include_reason=True,
    prompt_template="Return JSON with numeric 'score' (0 to 1) and 'reason'. Example: {\"score\":0.85,\"reason\":\"Context is precise with minor omissions.\"}"
)
