kw = {
    "Prompt on compelling photographic or email evidence returned": [
        ("evidence", r"evidence=([^\s]+)"),
        ("input_", r"input_=([^\s]+)"),
        ("is_compelling", r"is_compelling=([^\s]+)")
    ]
}
