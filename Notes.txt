from langchain.embeddings import HuggingFaceEmbeddings
from ragas.embeddings import LangchainEmbeddingsWrapper

# Step 1: Initialize an embedding model
evaluator_embedding = HuggingFaceEmbeddings(model_name="sentence-transformers/all-MiniLM-L6-v2")

# Step 2: Wrap it
wrapped_embedding = LangchainEmbeddingsWrapper(evaluator_embedding)

# Step 3: Pass to scorer
scorer = SemanticSimilarity(embeddings=wrapped_embedding)

# Step 4: Evaluate
await scorer.single_turn_a_score(sample)
