# Step 1: Define the keyword to match
target_keyword = 'Generated summary of compelling evidence'

# Step 2: Filter the row
row = d_final[d_final['keyword'] == target_keyword]

# Step 3: Extract the 'input' value (assume only one match)
input_json_string = row['input'].values[0]

# Step 4 (optional): Print it to confirm
print(input_json_string)
