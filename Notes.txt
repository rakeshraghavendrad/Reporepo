# Identify the row to update based on keyword
mask = d_final['keyword'] == target_keyword

# Replace the input column value in-place with the cleaned 'overall' text
d_final.loc[mask, 'input'] = overall
